node_modules/
dist/
*.tsbuildinfo
coverage/

.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

*.log
.env
.env.local

# large binary test corpus — keep out of the package tarball
dataset/
fyi

# agent session state
.dual-graph/
CONTEXT.md

# copied from the repository root at build time, so the two cannot drift
packages/a1sheet/README.md
packages/a1sheet/LICENSE

# storybook build output
storybook-static/
